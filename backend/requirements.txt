# Ad Server Simulator — backend dependencies
# Pins mirror the target ad server's proven-working set on Python 3.14
# (fastapi/pydantic/httpx versions verified against Voise-Adserver/backend/venv).

fastapi==0.110.1
uvicorn[standard]==0.25.0
pydantic==2.12.5
pydantic-settings>=2.5,<3
httpx==0.28.1
aiosqlite>=0.20.0
python-dotenv>=1.0.0
PyYAML>=6.0.1

# Tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
respx>=0.21.1
